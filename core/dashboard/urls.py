from django.contrib import admin
from django.urls import path,include
from . import views

app_name = "dashboard"

urlpatterns = [
    # path("profile/",views.DashboardIndex.as_view(),name="index"),
    # path("resume/",views.DescriptionView.as_view(),name="description-view"),
    # path("resume/create/",views.DescriptionView.as_view(),name="description-view"),
    # path("resume/profile/",views.SkillView.as_view(),name="skill-view"),

    
]

